<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.UserMapper">
    <select id="findByUsername" resultType="com.blog.entity.User">
        SELECT id, username, password, role, disable, create_time, update_time
        FROM blog_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(1) FROM blog_user
        WHERE username = #{username}
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
    </select>

    <update id="updatePassword">
        UPDATE blog_user SET password = #{password} WHERE id = #{id}
    </update>

    <update id="updateUsername">
        UPDATE blog_user SET username = #{username} WHERE id = #{id}
    </update>
</mapper>
