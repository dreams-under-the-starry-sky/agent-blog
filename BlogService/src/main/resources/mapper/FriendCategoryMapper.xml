<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.FriendCategoryMapper">
    <select id="selectAll" resultType="com.blog.entity.FriendCategory">
        SELECT id, name, sort, description, create_time, update_time
        FROM blog_friend_category
        ORDER BY id DESC
    </select>

    <select id="selectById" resultType="com.blog.entity.FriendCategory">
        SELECT id, name, sort, description, create_time, update_time
        FROM blog_friend_category WHERE id = #{id}
    </select>

    <select id="countByName" resultType="int">
        SELECT COUNT(1) FROM blog_friend_category
        WHERE name = #{name}
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
    </select>

    <insert id="insert">
        INSERT INTO blog_friend_category (id, name, sort, description)
        VALUES (#{id}, #{name}, #{sort}, #{description})
    </insert>

    <update id="update">
        UPDATE blog_friend_category
        SET name = #{name}, sort = #{sort}, description = #{description}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM blog_friend_category WHERE id = #{id}
    </delete>
</mapper>
