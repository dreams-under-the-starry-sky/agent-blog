<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.ArticleMapper">

    <sql id="BaseWhere">
        <where>
            <if test="keyword != null and keyword != ''">
                AND (a.title LIKE CONCAT('%', #{keyword}, '%') OR a.description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="categoryId != null">
                AND a.category_id = #{categoryId}
            </if>
            <if test="tagId != null">
                AND EXISTS (SELECT 1 FROM blog_article_tag t WHERE t.article_id = a.id AND t.tag_id = #{tagId})
            </if>
            <if test="status != null">
                AND a.status = #{status}
            </if>
            <if test="recommend != null">
                AND a.recommend = #{recommend}
            </if>
        </where>
    </sql>

    <select id="selectPage" resultType="com.blog.entity.Article">
        SELECT a.id, a.category_id, a.title, a.description, a.cover, a.thumbnail,
               a.comment, a.comments, a.status, a.recommend, a.pv,
               a.create_time, a.update_time, a.year_time, a.month_time,
               c.name AS category_name
        FROM blog_article a
        LEFT JOIN blog_category c ON a.category_id = c.id
        <include refid="BaseWhere"/>
        ORDER BY a.id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(1)
        FROM blog_article a
        <include refid="BaseWhere"/>
    </select>

    <select id="selectById" resultType="com.blog.entity.Article">
        SELECT a.id, a.category_id, a.title, a.description, a.cover, a.thumbnail,
               a.comment, a.comments, a.status, a.recommend, a.pv,
               a.create_time, a.update_time, a.year_time, a.month_time,
               c.name AS category_name,
               ac.content
        FROM blog_article a
        LEFT JOIN blog_category c ON a.category_id = c.id
        LEFT JOIN blog_article_content ac ON ac.article_id = a.id
        WHERE a.id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO blog_article (id, category_id, title, description, cover, thumbnail,
                                  comment, comments, status, recommend, pv, year_time, month_time)
        VALUES (#{id}, #{categoryId}, #{title}, #{description}, #{cover}, #{thumbnail},
                #{comment}, #{comments}, #{status}, #{recommend}, #{pv}, #{yearTime}, #{monthTime})
    </insert>

    <update id="update">
        UPDATE blog_article
        SET category_id = #{categoryId},
            title = #{title},
            description = #{description},
            cover = #{cover},
            thumbnail = #{thumbnail},
            comment = #{comment},
            status = #{status},
            recommend = #{recommend},
            year_time = #{yearTime},
            month_time = #{monthTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM blog_article WHERE id = #{id}
    </delete>

    <update id="incrementPv">
        UPDATE blog_article SET pv = IFNULL(pv, 0) + 1 WHERE id = #{id}
    </update>

    <update id="incrementComments">
        UPDATE blog_article SET comments = IFNULL(comments, 0) + #{delta} WHERE id = #{id}
    </update>

    <select id="sumPv" resultType="java.lang.Integer">
        SELECT IFNULL(SUM(pv), 0) FROM blog_article
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(1) FROM blog_article
    </select>

    <select id="countPublished" resultType="long">
        SELECT COUNT(1) FROM blog_article WHERE status = 1
    </select>

    <select id="countByCategory" resultType="java.util.HashMap">
        SELECT c.name AS name, COUNT(a.id) AS value
        FROM blog_category c
        LEFT JOIN blog_article a ON a.category_id = c.id AND a.status = 1
        GROUP BY c.id, c.name
        ORDER BY value DESC
    </select>

    <select id="selectRecent" resultType="com.blog.entity.Article">
        SELECT a.id, a.title, a.create_time, a.pv, a.cover
        FROM blog_article a
        WHERE a.status = 1
        ORDER BY a.create_time DESC
        LIMIT #{limit}
    </select>

    <select id="selectHot" resultType="com.blog.entity.Article">
        SELECT a.id, a.title, a.pv, a.cover
        FROM blog_article a
        WHERE a.status = 1
        ORDER BY a.pv DESC, a.create_time DESC
        LIMIT #{limit}
    </select>

    <select id="selectArchive" resultType="com.blog.entity.Article">
        SELECT a.id, a.title, a.create_time, a.year_time, a.month_time, a.pv, a.status
        FROM blog_article a
        WHERE IFNULL(a.status, 0) = 1
        ORDER BY a.create_time DESC
    </select>

    <select id="recountCategory" resultType="int">
        SELECT COUNT(1) FROM blog_article WHERE category_id = #{categoryId} AND status = 1
    </select>

    <select id="countByCategoryId" resultType="int">
        SELECT COUNT(1) FROM blog_article WHERE category_id = #{categoryId}
    </select>
</mapper>
