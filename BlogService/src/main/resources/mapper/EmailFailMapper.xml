<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.EmailFailMapper">
    <sql id="BaseWhere">
        <where>
            <if test="keyword != null and keyword != ''">
                AND (content LIKE CONCAT('%', #{keyword}, '%')
                     OR original_content LIKE CONCAT('%', #{keyword}, '%')
                     OR send_name LIKE CONCAT('%', #{keyword}, '%')
                     OR receive_name LIKE CONCAT('%', #{keyword}, '%')
                     OR receive_email LIKE CONCAT('%', #{keyword}, '%')
                     OR extra LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </sql>

    <select id="selectPage" resultType="com.blog.entity.EmailFail">
        SELECT id, kind, reply_id, parent_id, article_id, page_id, send_name, send_email,
               receive_name, receive_email, content, original_content, link, extra, create_time
        FROM blog_email_fail
        <include refid="BaseWhere"/>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(1) FROM blog_email_fail
        <include refid="BaseWhere"/>
    </select>

    <select id="selectById" resultType="com.blog.entity.EmailFail">
        SELECT id, kind, reply_id, parent_id, article_id, page_id, send_name, send_email,
               receive_name, receive_email, content, original_content, link, extra, create_time
        FROM blog_email_fail WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO blog_email_fail (kind, reply_id, parent_id, article_id, page_id, send_name, send_email,
                                     receive_name, receive_email, content, original_content, link, extra)
        VALUES (#{kind}, #{replyId}, #{parentId}, #{articleId}, #{pageId}, #{sendName}, #{sendEmail},
                #{receiveName}, #{receiveEmail}, #{content}, #{originalContent}, #{link}, #{extra})
    </insert>

    <update id="updateExtra">
        UPDATE blog_email_fail SET extra = #{extra} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM blog_email_fail WHERE id = #{id}
    </delete>
</mapper>
