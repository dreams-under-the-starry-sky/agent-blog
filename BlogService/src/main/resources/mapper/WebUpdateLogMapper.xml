<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.WebUpdateLogMapper">
    <select id="selectAll" resultType="com.blog.entity.WebUpdateLog">
        SELECT id, title, description, create_time, update_time
        FROM blog_web_update_log
        ORDER BY create_time DESC, id DESC
    </select>

    <insert id="insert">
        INSERT INTO blog_web_update_log (id, title, description)
        VALUES (#{id}, #{title}, #{description})
    </insert>

    <update id="update">
        UPDATE blog_web_update_log
        SET title = #{title}, description = #{description}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM blog_web_update_log WHERE id = #{id}
    </delete>
</mapper>
