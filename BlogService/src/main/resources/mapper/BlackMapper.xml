<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.BlackMapper">
    <select id="selectAll" resultType="com.blog.entity.Black">
        SELECT id, ip, position, nickname, email, create_time FROM blog_black ORDER BY id DESC
    </select>

    <select id="selectById" resultType="com.blog.entity.Black">
        SELECT id, ip, position, nickname, email, create_time FROM blog_black WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO blog_black (id, ip, position, nickname, email)
        VALUES (#{id}, #{ip}, #{position}, #{nickname}, #{email})
    </insert>

    <delete id="deleteById">
        DELETE FROM blog_black WHERE id = #{id}
    </delete>

    <select id="countMatch" resultType="int">
        SELECT COUNT(1) FROM blog_black
        <where>
            <if test="ip != null and ip != ''">
                OR ip = #{ip}
            </if>
            <if test="nickname != null and nickname != ''">
                OR nickname = #{nickname}
            </if>
            <if test="email != null and email != ''">
                OR email = #{email}
            </if>
        </where>
    </select>
</mapper>
